# wiki-local Relocation Note

`wiki-local` no longer lives in `Three-Realms-Protocol/tools/wiki-local/`.

Current stack home:

- `../Three-Realms-Academy/tools/wiki-local/`

Boundary reminder:

- `Three-Realms-Academy/tools/wiki-local/` owns the installable wiki stack, seed deployment layer, and navigation tooling.
- `Three-Realms-Protocol/` remains the canonical source body for `DOCS/wiki`, `LEX`, `SPEC`, `EPOCH`, and related protocol texts.
- `Three-Quarters-International/IDENTITY/` remains the shared auth and tunnel layer used by the wiki.
